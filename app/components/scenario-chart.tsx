import {Table,TableHeader,TableBody,TableRow,TableHead,TableCell,TableCaption} from '@/components/ui/table';
const rows=[['Base case',100,35,30,10,25],['Funding pressure',100,45,30,10,15],['Credit pressure',100,35,30,20,15]] as const;
export function ScenarioChart(){return <figure className="chart"><figcaption>How cost assumptions change the operating result</figcaption><p className="synthetic-label"><strong>Synthetic data · Illustrative scenarios · Index units</strong><br/>No market observation date applies. Prepared for this website sample.</p><div aria-label="Operating result: base case 25 index units; funding pressure 15; credit pressure 15.">{rows.map(r=><div className="bar-row" key={r[0]}><span>{r[0]}</span><div className="bar-track"><div className="bar-fill" style={{width:(r[5]/30*100)+'%'}}/></div><span>{r[5]}</span></div>)}</div><Table className="data-table"><TableCaption>Synthetic assumptions. Result = income − funding − operating costs − credit charges. Taxes and other items excluded.</TableCaption><TableHeader><TableRow>{['Scenario','Income','Funding costs','Operating costs','Credit charges','Result'].map(t=><TableHead key={t} scope="col">{t}</TableHead>)}</TableRow></TableHeader><TableBody>{rows.map(r=><TableRow key={r[0]}>{r.map((v,i)=>i===0?<TableHead scope="row" key={i}>{v}</TableHead>:<TableCell key={i}>{v}</TableCell>)}</TableRow>)}</TableBody></Table><p className="small">Source: original illustrative model. All values are assumptions, not reported facts or analyst estimates for a real issuer.</p></figure>;}

