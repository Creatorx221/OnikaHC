# Public content is the default. Use preview only for an intentional demonstration.
SITE_MODE=public
# Production CMS_ADMIN_USER_IDS belongs in Sites environment settings.
# Use a verified site-specific account ID, never an email address.
